## 1. Home Metadata And Copy

- [x] 1.1 Refine Spanish Home title, meta description, hero copy, and catalog intro for Granada guided-tour intent, and verify `npm run check` passes.
- [x] 1.2 Refine English Home title, meta description, hero copy, and catalog intro for Granada guided-tour intent, and verify `npm run check` passes.

## 2. Home Internal Discovery

- [x] 2.1 Expand Spanish and English Home planning links to published SEO landing pages for major tourism intents, and verify `npm run build` succeeds.
- [x] 2.2 Keep the Home planning `ItemList` JSON-LD aligned with visible planning links, and verify generated Home HTML includes the expanded landing links.

## 3. Business Structured Data

- [x] 3.1 Add a business-level service catalog JSON-LD using approved public service facts and existing landing URLs, and verify generated Home HTML includes `OfferCatalog`.
- [x] 3.2 Confirm the structured data does not add fake reviews, aggregate ratings, unapproved addresses, business hours, awards, credentials, ranking guarantees, or internal booking/payment claims.

## 4. Verification

- [x] 4.1 Run `npm run check` and verify there are no Astro or TypeScript diagnostics.
- [x] 4.2 Run `npm run build` and verify the static site builds successfully.
