## Why

The Spanish tour `1492 Nuevo Orden Mundial` needs its public imagery to match the provided Spanish-language tour identity artwork. Using the approved image makes the Spanish catalog and detail presentation consistent with the tour name and avoids showing the generic Granada cathedral image.

## What Changes

- Add or reuse the provided Spanish `1492 Nuevo Orden Mundial` artwork as a public tour image asset in a web-optimized format suitable for the static site.
- Update only the Spanish `1492 Nuevo Orden Mundial` tour entry to use that image in its `image` metadata.
- Ensure the updated image appears anywhere the existing shared tour catalog and detail page patterns render that Spanish tour.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `static-tourism-site`: The Spanish `1492 Nuevo Orden Mundial` tour must display the provided approved Spanish artwork as its public tour image.

## Impact

- Affected content: the Spanish tour content entry for `1492 Nuevo Orden Mundial`.
- Affected assets: one Spanish public image asset, preferably derived into a WebP or AVIF file.
- Affected UI: existing Spanish catalog cards and tour detail pages that render the tour's `image` field.
- No API, dependency, routing, booking, payment, or backend changes.
