## 1. Localized Content And Routing

- [x] 1.1 Add localized contact page copy for Spanish and English, including title, intro, purpose text, channel labels, CTA labels, and SEO descriptions; verify both locales expose complete text without TypeScript errors.
- [x] 1.2 Add a localized contact route helper or equivalent slug mapping for `/es/contacto/` and `/en/contact/`; verify generated links use the correct locale-specific URL.

## 2. Contact Page Implementation

- [x] 2.1 Create localized static contact page route(s); verify `/es/contacto/` and `/en/contact/` are generated by the production build.
- [x] 2.2 Build the page using the existing tourism visual language and a simple contact-page structure inspired by pateandogranada.net/contacto; verify the page has a clear hero/intro and contact cards.
- [x] 2.3 Display only `elyaratours@gmail.com` and Instagram `@elyaratours`; verify no WhatsApp, phone, physical address, or extra social networks appear on the contact page.
- [x] 2.4 Link email to `mailto:elyaratours@gmail.com` and Instagram to `https://instagram.com/elyaratours`; verify the generated pages contain the expected URLs and external-link attributes for Instagram.
- [x] 2.5 Add explanatory copy for route reservations, private routes, tailor-made trip planning, and questions about visiting Granada; verify both localized pages include these contact reasons.
- [x] 2.6 Add localized SEO metadata, canonical paths, and language alternates for the contact pages; verify generated page head output includes the expected localized metadata.

## 3. Shared Navigation

- [x] 3.1 Add localized contact navigation from the header; verify Spanish links to `/es/contacto/` and English links to `/en/contact/`.
- [x] 3.2 Add or update footer contact discovery so visitors can reach the localized contact page while preserving the existing email and Instagram footer links; verify footer Blog, ES, and EN links remain present.

## 4. Scope Guardrails

- [x] 4.1 Confirm no contact form, newsletter signup, backend endpoint, storage, account creation, checkout, payment flow, or new dependency is introduced; verify source and package files contain no new related implementation.

## 5. Verification

- [x] 5.1 Run `npm run check` and verify it succeeds.
- [x] 5.2 Run `npm run build` and verify it succeeds with the localized contact pages generated.
- [x] 5.3 Run `openspec validate add-contact-page --type change --strict` and verify the change is valid.
