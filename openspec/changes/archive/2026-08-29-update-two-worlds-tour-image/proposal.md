## Why

The `Granada entre dos mundos` / `Granada Between Two Worlds` tour pair should use the newly provided `2mundos.jpeg` image instead of the current `centro-albayzin.jpg` image. The replacement better matches the route's atmosphere and needs localized accessible text that describes the new window view.

## What Changes

- Update the Spanish and English tour entries for the Two Worlds route to reference `/images/2mundos.jpeg`.
- Update localized image alternative text for both tour entries to describe the new view from a window toward Granada gardens and historic rooftops.
- Preserve the existing tour routes, titles, descriptions, pricing, reservation links, SEO descriptions, ordering, and localized catalog/detail behavior.

## Capabilities

### New Capabilities

- None.

### Modified Capabilities

- `static-tourism-site`: change the approved public image for the Spanish `Granada entre dos mundos` tour and the English `Granada Between Two Worlds` tour from `/images/centro-albayzin.jpg` to `/images/2mundos.jpeg`.

## Impact

- Affected areas: localized Two Worlds tour content metadata, tour catalog cards, tour detail hero image, Open Graph image metadata, and tour JSON-LD image metadata.
- No new dependencies, routes, content collections, or booking behavior changes are expected.
- Other tour imagery should remain unchanged.
