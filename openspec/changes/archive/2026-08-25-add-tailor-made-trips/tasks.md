## 1. Localized Content And Routing

- [x] 1.1 Add localized copy for the tailor-made home section, standalone page, service cards, CTA labels, SEO descriptions, and WhatsApp inquiry text; verify both `es` and `en` locales expose complete text without TypeScript errors.
- [x] 1.2 Add a localized route helper or equivalent slug mapping for `/es/viajes-a-medida/` and `/en/tailor-made-trips/`; verify generated links use the correct locale-specific URL.

## 2. Home Discovery Section

- [x] 2.1 Add the tailor-made discovery block after the tour catalog and before traveler reviews on the localized home page; verify the Spanish home shows `Viajes y rutas disenadas para ti` in that position.
- [x] 2.2 Populate the home block with copy that introduces both consultation and personalized Granada routes; verify the English home shows `Tailor-made routes for your trip` and links to `/en/tailor-made-trips/`.
- [x] 2.3 Ensure the home block uses existing visual patterns and remains responsive on mobile and desktop; verify layout by running the site locally or inspecting the built pages.

## 3. Standalone Tailor-Made Pages

- [x] 3.1 Create the localized standalone tailor-made page route(s); verify `/es/viajes-a-medida/` and `/en/tailor-made-trips/` are generated by the production build.
- [x] 3.2 Add page content that clearly distinguishes travel consultation from personalized guided routes in Granada; verify both localized pages contain the two service paths.
- [x] 3.3 Add inspiration examples for personalized Granada routes without presenting them as fixed catalog tours; verify examples do not require tour metadata such as fixed duration, price, or reservation fields.
- [x] 3.4 Add localized SEO metadata, canonical paths, and language alternates for the standalone pages; verify page head output includes the expected localized metadata.

## 4. External Inquiry Flow

- [x] 4.1 Add WhatsApp inquiry CTAs on the home section and standalone pages using prefilled messages that ask for dates, group size, interests, and language; verify links are external and open the expected WhatsApp URL.
- [x] 4.2 Confirm no internal form, account creation, checkout, payment, or backend integration is introduced; verify the implementation contains no new form submission endpoint or payment dependency.

## 5. Verification

- [x] 5.1 Run `npm run check` and verify it succeeds.
- [x] 5.2 Run `npm run build` and verify it succeeds with the localized tailor-made pages generated.
- [x] 5.3 Run `openspec validate add-tailor-made-trips --type change --strict` and verify the change remains valid after implementation task statuses are updated.
